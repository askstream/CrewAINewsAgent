# CrewAI News Agent

Система агентов на базе CrewAI для автоматического сбора новостей из RSS каналов, дедупликации и интеллектуальной классификации по релевантности с использованием LLM.

## Основной функционал

### 🔍 Сбор новостей из RSS каналов
- Автоматический парсинг RSS/Atom фидов
- Извлечение заголовков, содержания, дат публикации и ссылок
- Поддержка множественных RSS источников
- Автоматическое определение источника новости
- Сохранение метаданных (дата публикации, источник)

### 🔄 Дедупликация статей
- Двухуровневая система поиска дубликатов:
  - **Быстрая проверка по хешу** - точные дубликаты (идентичное содержание)
  - **Анализ схожести текста** - поиск похожих статей с использованием алгоритма SequenceMatcher
- Настраиваемый порог схожести (по умолчанию 0.85)
- Сохранение связи между оригиналом и дубликатом
- Автоматическое определение более ранней версии как оригинала

### 🎯 Классификация по релевантности
- Интеллектуальная оценка релевантности с использованием LLM (OpenAI API)
- Поддержка кастомных API endpoints (через OPENAI_API_BASE)
- Fallback на простую классификацию по ключевым словам при ошибках API
- Оценка релевантности от 0.0 до 1.0
- Автоматическое определение релевантности (порог ≥ 0.6)
- Сохранение причины классификации для каждой статьи

### 📝 Автоматическое саммари статей
- Генерация кратких саммари (2-3 предложения) для релевантных статей
- Использование LLM для создания информативных саммари
- Автоматическое отображение саммари в интерфейсе
- Fallback на простое саммари из первых предложений при ошибках API

### 🔍 Семантический поиск
- Векторные представления (embeddings) для всех статей
- Семантический поиск по смыслу, а не только по ключевым словам
- Настраиваемый порог схожести (0.0-1.0)
- Поиск похожих статей по векторному сходству
- Отображение оценки схожести в результатах поиска

### 📊 Веб-интерфейс
- Интуитивный интерфейс на Bootstrap 5 с вкладками
- **Вкладка "Поиск"** - основная рабочая область:
  - Отслеживание прогресса в реальном времени:
    - Визуальные индикаторы этапов обработки
    - Прогресс-бары для каждого этапа
    - Детальные сообщения о текущем статусе
  - **Семантический поиск**:
    - Поиск статей по смыслу, а не только по ключевым словам
    - Настраиваемый порог схожести
    - Отображение оценки схожести в результатах
  - Таблица новостей текущего поиска:
    - Отображение только статей из текущего запроса
    - Визуальное выделение релевантных статей (зеленый фон)
    - Группировка строк по статьям
    - **Автоматическое саммари** для релевантных статей
    - Отображение HTML-разметки в содержании
    - Показ причин классификации
  - Итоговая статистика:
    - Общее количество статей текущего запроса
    - Количество релевантных
    - Количество дубликатов
    - Количество уникальных нерелевантных
  - Кнопка "Удалить данные запроса" (активна после завершения поиска)
- **Вкладка "История запросов"**:
  - Таблица истории запросов с пагинацией (5 записей на страницу)
  - Форма с деталями выбранной записи истории:
    - Все параметры запроса (RSS каналы, критерий, настройки LLM)
    - Статистика результатов
  - Таблица статей выбранного запроса
  - Кнопка "Очистить базу данных" с подтверждением
- **Вкладка "Системные настройки"**:
  - Управление настройками семантического поиска через веб-интерфейс
  - Редактирование адаптивных порогов схожести для разных длин запросов
  - Настройка параметров keyword matching (минимальный процент совпадений, вес буста)
  - Сохранение изменений в базу данных
  - Кнопка "Инициализировать настройки по умолчанию" для сброса к исходным значениям
  - Все настройки хранятся в таблице `system_settings` и применяются динамически
- Автоматическое обновление данных после обработки

## Установка

### Требования
- Python 3.8+
- pip

### Шаги установки

1. Клонируйте репозиторий или скачайте проект

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` на основе примера:
```bash
# Скопируйте пример
cp env.example .env
```

4. Настройте переменные окружения в `.env`:

**Обязательные параметры:**
- `OPENAI_API_KEY` - ваш API ключ OpenAI или альтернативного провайдера
  - **Для OpenAI**: укажите ваш реальный ключ (начинается с `sk-...`)
  - **Для Ollama**: можно указать любое значение, например `ollama` (Ollama игнорирует этот параметр, но библиотека требует его наличие)
  - **Для других провайдеров**: укажите ключ, предоставленный провайдером
- `RSS_FEEDS` - список RSS каналов через запятую (для CLI режима)
- `SELECTION_CRITERIA` - критерий отбора новостей (для CLI режима)

**Опциональные параметры:**
- `OPENAI_API_BASE` - базовый URL API (для использования альтернативных провайдеров, например: `https://api.proxyapi.ru/openai/v1`)
  - Настраивается только через `.env`, не изменяется динамически из веб-интерфейса
  - **Важно для embeddings**: API должен поддерживать endpoint `/v1/embeddings` для работы семантического поиска
- `LLM_MODEL` - модель LLM (по умолчанию: `gpt-4o-mini`)
- `LLM_TEMPERATURE` - температура для LLM (по умолчанию: `0.7`)
- `EMBEDDING_MODEL` - модель для генерации embeddings (по умолчанию: `text-embedding-3-small`)
  - Для Ollama используйте: `snowflake-arctic-embed2` или `granite-embedding`
- `DATABASE_URL` - URL базы данных (по умолчанию: `sqlite:///data/news_agent.db`)
  - База данных автоматически создается в поддиректории `data`
- `SIMILARITY_THRESHOLD` - порог схожести для дедупликации от 0.0 до 1.0 (по умолчанию: `0.85`)
- `FLASK_HOST` - хост для веб-сервера (по умолчанию: `127.0.0.1`)
- `FLASK_PORT` - порт для веб-сервера (по умолчанию: `5000`)

**Примечание о функциях:**
- Саммари и embeddings генерируются автоматически при обработке новостей
- Если API не поддерживает embeddings (404 ошибка), система продолжит работу без семантического поиска
- Саммари генерируется только для релевантных статей для экономии API-вызовов

## Использование

### Веб-интерфейс (рекомендуется)

Запустите Flask приложение:
```bash
python app.py
```

Откройте браузер и перейдите по адресу:
```
http://127.0.0.1:5000
```

#### Возможности веб-интерфейса:

**Блок текущих настроек системы:**
- Отображение текущих значений из конфигурации:
  - Модель LLM
  - Temperature
  - Порог схожести для дедупликации
  - API Endpoint
- Настройки загружаются автоматически при открытии страницы

**Форма настройки обработки:**
- Поле для ввода RSS каналов (каждый канал с новой строки)
- Поле для указания критерия отбора новостей
- **Настройки LLM и обработки (можно изменить для текущего запуска):**
  - Модель LLM (текстовое поле, например: `gpt-4o-mini`)
  - Temperature (числовое поле, 0.0-2.0)
  - Порог схожести для дедупликации (числовое поле, 0.0-1.0)
- Кнопка "Поиск" для запуска процесса
- Кнопка "Удалить данные запроса" (появляется после завершения поиска)

**Важно:** 
- Настройки в форме применяются только для текущего запуска обработки и не изменяют значения в `.env`
- API Endpoint настраивается только через `.env` и не изменяется динамически
- Каждый поиск создает новую запись в истории запросов
- На главной вкладке отображаются только статьи текущего поиска

**Отслеживание прогресса:**
- Этапы обработки с визуальными индикаторами:
  1. **Сбор новостей из RSS каналов** - показывает количество собранных статей
  2. **Дедупликация статей** - показывает количество найденных дубликатов
  3. **Классификация по релевантности** - показывает прогресс обработки каждой статьи
     - Включает генерацию саммари для релевантных статей
     - Включает генерацию векторных представлений (embeddings) для всех статей
- Обновление статуса каждую секунду
- Цветовая индикация статуса (ожидание/выполнение/завершено/ошибка)
- Детальные сообщения о текущем статусе каждого этапа

**Таблица новостей (на вкладке "Поиск"):**
- Автоматическая загрузка при открытии страницы
- Отображение только статей текущего поиска (без дубликатов)
- Колонки: ID, Источник, Дата, Заголовок, Релевантность
- Дополнительные строки для каждой статьи:
  - **Саммари** (для релевантных статей) - краткое содержание в информационном блоке
  - Полное содержание с сохранением HTML-разметки (с возможностью развернуть/свернуть)
  - Причина классификации (если указана)
- Визуальное выделение релевантных статей зеленым фоном
- Группировка строк по статьям с цветной границей
- Прокручиваемая таблица (максимальная высота 1200px)

**Итоговая статистика:**
- Отображается после завершения обработки
- Показывает статистику текущего запроса: общее количество статей, релевантных, дубликатов и нерелевантных

**Вкладка "История запросов":**
- Таблица истории запросов с пагинацией (5 записей на страницу)
- При клике на запись:
  - Отображается форма с деталями запроса (все параметры и статистика)
  - Загружается таблица статей этого запроса
- По умолчанию выбирается последний (самый свежий) запрос
- Кнопка "Очистить базу данных" для полной очистки всех данных

**Вкладка "Системные настройки":**
- Управление настройками семантического поиска через веб-интерфейс
- Редактирование адаптивных порогов схожести для разных длин запросов:
  - Порог для запросов из 1 слова
  - Порог для запросов из 2 слов
  - Порог для запросов из 3 слов
  - Порог для запросов из 4-5 слов
  - Порог для запросов из 6+ слов
- Настройка параметров keyword matching:
  - Минимальный процент совпадений слов
  - Вес буста для keyword matches
  - Минимальный общий счет для keyword matches
- Сохранение изменений в базу данных (применяются сразу)
- Кнопка "Инициализировать настройки по умолчанию" для сброса к исходным значениям
- Все настройки хранятся в таблице `system_settings` и применяются динамически при каждом поиске

### Командная строка

Альтернативно, можно запустить обработку через командную строку:
```bash
python main.py
```

В этом случае настройки берутся из файла `.env`.

## Процесс обработки

Система выполняет обработку новостей в несколько этапов:

### Этап 1: Сбор новостей
1. Парсинг каждого указанного RSS канала
2. Извлечение данных из каждой записи:
   - Заголовок
   - Содержание (summary или description)
   - Ссылка на полную статью
   - Дата публикации
   - Название источника
3. Генерация хеша для быстрой проверки дубликатов
4. Проверка на существование статьи в базе (по ссылке)
5. Сохранение новых статей в базу данных

### Этап 2: Дедупликация
1. Получение всех необработанных статей из базы
2. Быстрая проверка по хешу содержимого (точные дубликаты)
3. Анализ схожести текста для похожих статей:
   - Сравнение заголовков
   - Сравнение содержания
   - Использование алгоритма SequenceMatcher
4. Определение оригинальной статьи (более ранняя дата публикации)
5. Пометка дубликатов в базе данных

### Этап 3: Классификация
1. Получение уникальных статей (не дубликаты)
2. Для каждой статьи:
   - Формирование промпта с критерием отбора
   - Отправка запроса к LLM API
   - Парсинг JSON ответа с оценкой релевантности
   - Fallback на простую классификацию при ошибках
3. Сохранение результатов:
   - Оценка релевантности (0.0 - 1.0)
   - Флаг релевантности (true/false)
   - Причина классификации

### Этап 4: Генерация саммари (опционально)
1. **Загрузка релевантных статей из базы данных**:
   - Фильтрация статей по `search_history_id`, `is_relevant == True` и `is_duplicate == False`
   - Загрузка статей из текущей сессии БД для избежания проблем с detached объектами

2. **Для каждой релевантной статьи**:
   - **Очистка HTML**: Удаление HTML-тегов из содержания статьи с помощью регулярных выражений
   - **Подготовка текста**: Ограничение длины текста для оптимизации запросов к API
   - **Формирование промпта**: Создание промпта для LLM с инструкцией сгенерировать краткое саммари (2-3 предложения)
   - **Отправка запроса к LLM API**:
     - Используется прямой HTTP запрос к API (если указан `OPENAI_API_BASE`)
     - Или через LangChain (fallback)
     - Используются настройки `llm_model` и `llm_temperature` из текущего запроса
   - **Fallback механизм**: При ошибках API используется простое саммари из первых предложений текста

3. **Сохранение результатов**:
   - Саммари сохраняется в поле `summary` таблицы `news_articles`
   - Обновление выполняется через SQLAlchemy ORM с использованием `session.merge()`
   - Транзакция коммитится после обработки всех статей

**Технические детали**:
- Саммари генерируется только для релевантных статей для экономии API-вызовов
- Используется модуль `agents/summarizer.py` с функциями:
  - `generate_summary_with_direct_api()` - прямой HTTP запрос
  - `generate_summary_with_langchain()` - через LangChain
  - `generate_simple_summary()` - fallback на простое саммари
- Ошибки обрабатываются gracefully: процесс продолжается даже при сбоях

### Этап 5: Генерация embeddings (опционально)
1. **Подготовка списка статей**:
   - Получение ID всех уникальных статей текущего запроса до закрытия сессии БД
   - Сохранение ID в список `unique_article_ids` для передачи в функцию генерации

2. **Для каждой статьи (по ID)**:
   - **Загрузка статьи из текущей сессии БД**: Статья загружается заново по ID из новой сессии
   - **Получение значений атрибутов**: 
     - Извлечение `title` и `content` сразу после загрузки (до закрытия сессии)
     - Преобразование в строки для избежания проблем с detached объектами
   - **Подготовка текста для embedding**:
     - Комбинирование заголовка и содержания
     - Очистка HTML из содержания (удаление тегов, декодирование HTML entities)
     - Ограничение длины текста до 2000 символов для оптимизации
   - **Генерация векторного представления**:
     - Отправка запроса к OpenAI Embeddings API (модель `text-embedding-3-small` по умолчанию)
     - Использование `OPENAI_API_BASE` если указан, иначе стандартный OpenAI endpoint
     - Получение вектора размером 1536 чисел (для `text-embedding-3-small`)
   - **Проверка существования**: Генерация выполняется только если embedding еще не существует

3. **Сохранение результатов**:
   - Embedding сохраняется в поле `embedding` таблицы `news_articles` (тип JSON)
   - Обновление выполняется через SQLAlchemy ORM
   - Транзакция коммитится после обработки всех статей

**Технические детали**:
- Используется модуль `agents/embeddings.py` с функцией `generate_embeddings_for_articles_by_ids()`
- Функция принимает список ID статей вместо объектов для избежания проблем с сессиями БД
- Обработка ошибок:
  - Если API не поддерживает embeddings (404), процесс продолжается без ошибок
  - Если статья не найдена или нет текста, она пропускается
  - Все ошибки логируются, но не прерывают процесс
- Embeddings используются для семантического поиска: поиск статей по смыслу, а не только по ключевым словам

**Как работает семантический поиск**:
1. Пользователь вводит текстовый запрос (например: "стоимость визы в Индию")
2. Система фильтрует стоп-слова и извлекает значимые слова из запроса
3. **Основной механизм - семантический поиск через embeddings**:
   - Генерируется embedding для запроса через OpenAI Embeddings API
   - Для каждой статьи с embedding вычисляется косинусное сходство между вектором запроса и вектором статьи
   - Используется адаптивный порог в зависимости от длины запроса (настраивается через вкладку "Системные настройки"):
     * 1 слово → порог по умолчанию 0.25 (настраивается)
     * 2 слова → порог по умолчанию 0.3 (настраивается)
     * 3 слова → порог по умолчанию 0.35 (настраивается)
     * 4-5 слов → порог по умолчанию 0.4 (настраивается)
     * 6+ слов → порог по умолчанию 0.5 (настраивается)
4. **Дополнительный механизм - keyword matching**:
   - Ищет точные совпадения слов в заголовке, содержании и саммари
   - Используется как буст для статей, найденных через embeddings (+0.1 к similarity)
   - Также добавляет статьи без embeddings, если найдено ≥50% слов запроса
5. Результаты объединяются: сначала semantic results, затем keyword matches
6. Статьи сортируются по убыванию схожести
7. Результаты отображаются с оценкой схожести (similarity_score)

**Преимущества семантического поиска**:
- **Поиск по смыслу, а не только по ключевым словам** - основной механизм
- Находит статьи даже если они используют другие формулировки (например: "стоимость визы" найдет статью про "сбор за визу")
- Работает с синонимами и связанными понятиями автоматически через embeddings
- Адаптивный порог для разных длин запросов обеспечивает лучшие результаты (настраивается через "Системные настройки")
- Keyword matching используется как дополнительный буст, а не основной фильтр
- Можно искать на разных языках (если используется multilingual модель)
- Все параметры поиска настраиваются через веб-интерфейс без изменения кода

## Структура проекта

```
CrewAINewsAgent/
├── app.py                 # Flask веб-приложение с API endpoints
├── main.py               # Точка входа для CLI режима
├── config.py             # Конфигурация (загрузка из .env)
├── models.py             # Модели базы данных (SQLAlchemy)
├── crew.py               # Оркестрация агентов CrewAI
├── requirements.txt      # Зависимости Python
├── env.example           # Пример файла конфигурации
├── README.md             # Документация
├── data/                 # Директория для базы данных
│   └── news_agent.db     # SQLite база данных (создается автоматически)
└── agents/               # Модули агентов
    ├── __init__.py
    ├── rss_collector.py  # Агент сбора новостей из RSS
    ├── deduplicator.py   # Агент дедупликации статей
    ├── classifier.py     # Агент классификации по релевантности
    ├── summarizer.py     # Агент генерации саммари статей
    ├── embeddings.py     # Модуль для работы с векторными представлениями
    └── llm_utils.py      # Утилиты для работы с LLM
└── templates/
    └── index.html        # Веб-интерфейс
└── static/
    ├── css/
    │   └── style.css     # Стили интерфейса
    └── js/
        └── app.js        # JavaScript логика интерфейса
```

### Описание модулей

**app.py** - Flask приложение:
- REST API для запуска обработки и получения статуса
- Веб-интерфейс с отслеживанием прогресса
- Управление задачами в фоновых потоках
- API endpoints:
  - `/api/start` - запуск обработки новостей
  - `/api/status/<task_id>` - получение статуса задачи
  - `/api/results` - получение результатов обработки
  - `/api/semantic-search` - семантический поиск статей
  - `/api/search-history` - получение истории запросов
  - `/api/search-history/<id>/articles` - статьи конкретного запроса
  - `/api/search-history/<id>` (DELETE) - удаление запроса
  - `/api/statistics` - общая статистика
  - `/api/clear-db` (POST) - очистка базы данных
  - `/api/settings` (GET) - получение системных настроек (с опциональным фильтром по категории)
  - `/api/settings` (POST) - обновление системных настроек
  - `/api/settings/init` (POST) - инициализация настроек по умолчанию

**models.py** - Модели данных:
- `NewsArticle` - модель новостной статьи с полями:
  - Основные: id, title, content, link, source, published_at
  - Связь: search_history_id (связь с историей запросов)
  - Обработка: is_duplicate, duplicate_of, relevance_score, is_relevant, classification_reason
  - Технические: content_hash, collected_at
- `SearchHistory` - модель истории запросов:
  - Параметры: rss_feeds, selection_criteria, llm_model, llm_temperature, similarity_threshold, openai_api_base
  - Результаты: results_data (JSON с статистикой)
  - Связь: articles (связанные статьи через relationship)
- `RSSFeed` - модель RSS канала (зарезервировано для будущего использования)

**agents/rss_collector.py**:
- Функция `collect_rss_news()` - парсинг RSS каналов
- Функция `create_rss_collector_agent()` - создание CrewAI агента
- Использует библиотеку `feedparser` для парсинга

**agents/deduplicator.py**:
- Функция `find_duplicates()` - поиск дубликатов
- Функция `calculate_similarity()` - вычисление схожести текста
- Функция `mark_duplicates()` - пометка дубликатов в БД
- Функция `create_deduplicator_agent()` - создание CrewAI агента

**agents/classifier.py**:
- Функция `classify_article_relevance()` - классификация с LLM
- Функция `classify_with_direct_api()` - прямой HTTP запрос к API
- Функция `simple_classification()` - fallback классификация по ключевым словам
- Функция `classify_articles()` - пакетная классификация
- Функция `create_classifier_agent()` - создание CrewAI агента

**agents/llm_utils.py**:
- Функция `create_llm()` - создание LLM клиента с правильной обработкой base_url
- Функция `create_llm_with_settings()` - создание LLM с указанными настройками
- Поддержка кастомных API endpoints

**agents/summarizer.py**:
- Функция `generate_summary()` - основная функция генерации саммари
  - Пробует прямой API запрос, затем LangChain, затем простое саммари
- Функция `generate_summary_with_direct_api()` - генерация через прямой HTTP запрос к API
  - Формирует промпт с инструкцией создать краткое саммари (2-3 предложения)
  - Использует настройки `llm_model` и `llm_temperature`
  - Поддержка кастомных API endpoints
- Функция `generate_summary_with_langchain()` - генерация через LangChain
  - Fallback если прямой API запрос не работает
- Функция `generate_simple_summary()` - fallback на простое саммари
  - Берет первые предложения из текста статьи
  - Используется при ошибках API
- Функция `clean_html()` - очистка HTML из текста
  - Удаление HTML тегов с помощью регулярных выражений
  - Декодирование HTML entities
- Функция `generate_summaries_for_articles()` - пакетная генерация саммари
  - Обрабатывает список статей
  - Генерирует саммари только если его еще нет
  - Сохраняет результаты в БД через SQLAlchemy ORM

**agents/embeddings.py**:
- Функция `generate_embedding_with_openai()` - генерация embeddings через OpenAI API
  - Поддержка кастомных API endpoints через `OPENAI_API_BASE`
  - Обработка ошибок (404 для неподдерживаемых API)
  - Ограничение длины текста до 8000 символов
- Функция `clean_text()` - очистка текста от HTML тегов и лишних символов
- Функция `cosine_similarity()` - вычисление косинусного сходства между двумя векторами
  - Использует NumPy для векторных операций
  - Возвращает значение от 0.0 до 1.0
- Функция `find_similar_articles()` - поиск похожих статей по embedding
  - Фильтрация по порогу схожести
  - Сортировка по убыванию схожести
- Функция `semantic_search()` - семантический поиск по текстовому запросу
  - Генерирует embedding для запроса
  - Загружает статьи из БД с фильтрацией по `search_history_id` (опционально)
  - Возвращает список кортежей `(article_data, similarity)` с данными статей
  - Получает все данные статей до закрытия сессии БД
- Функция `generate_embeddings_for_articles_by_ids()` - пакетная генерация embeddings по ID статей
  - Принимает список ID вместо объектов для избежания проблем с сессиями БД
  - Загружает статьи из текущей сессии
  - Получает значения атрибутов сразу после загрузки
  - Генерирует embedding только если его еще нет
- Функция `generate_embeddings_for_articles()` - обертка для обратной совместимости

## База данных

### Структура

По умолчанию используется SQLite (`data/news_agent.db`). База данных создается автоматически в поддиректории `data` при первом запуске.

**Таблица `news_articles`:**
- `id` - уникальный идентификатор
- `title` - заголовок статьи
- `content` - содержание статьи (может содержать HTML)
- `link` - ссылка на полную статью
- `source` - источник новости (название RSS канала)
- `published_at` - дата публикации
- `collected_at` - дата сбора новости
- `search_history_id` - связь с историей запросов (ForeignKey)
- `is_duplicate` - флаг дубликата
- `duplicate_of` - ID оригинальной статьи (если дубликат)
- `relevance_score` - оценка релевантности (0.0 - 1.0)
- `is_relevant` - флаг релевантности
- `classification_reason` - причина классификации
- `content_hash` - SHA256 хеш для быстрой проверки дубликатов
- `summary` - краткое саммари статьи (генерируется автоматически для релевантных статей)
- `embedding` - векторное представление статьи (JSON массив чисел) для семантического поиска
- Уникальный индекс: `(link, search_history_id)` - одна статья может быть в разных запросах

**Таблица `search_history`:**
- `id` - уникальный идентификатор
- `created_at` - дата/время создания запроса
- `rss_feeds` - RSS каналы (каждый с новой строки)
- `selection_criteria` - критерий отбора новостей
- `llm_model` - модель LLM, использованная для классификации
- `llm_temperature` - температура LLM
- `similarity_threshold` - порог схожести для дедупликации
- `openai_api_base` - API endpoint (если использовался)
- `results_data` - JSON с результатами и статистикой:
  - total, relevant, duplicates, unique_non_relevant
  - collected_articles, processed_articles
  - другие метаданные

**Таблица `rss_feeds`:**
- Зарезервирована для будущего использования
- Планируется хранение списка RSS каналов в БД

**Таблица `system_settings`:**
- `id` - уникальный идентификатор
- `key` - ключ настройки (уникальный, индексируется)
- `value` - значение настройки (текст)
- `description` - описание настройки
- `category` - категория настройки (например: `semantic_search`)
- `updated_at` - дата/время последнего обновления
- **Настройки семантического поиска:**
  - `semantic_threshold_1_word` - порог для запросов из 1 слова (по умолчанию: 0.25)
  - `semantic_threshold_2_words` - порог для запросов из 2 слов (по умолчанию: 0.3)
  - `semantic_threshold_3_words` - порог для запросов из 3 слов (по умолчанию: 0.35)
  - `semantic_threshold_4_5_words` - порог для запросов из 4-5 слов (по умолчанию: 0.4)
  - `semantic_threshold_6_plus_words` - порог для запросов из 6+ слов (по умолчанию: 0.5)
  - `keyword_match_min_ratio` - минимальный процент совпадений слов для keyword matching (по умолчанию: 0.5)
  - `keyword_boost_weight` - вес буста для keyword matches (по умолчанию: 0.1)
  - `keyword_match_min_score` - минимальный общий счет для keyword matches (по умолчанию: 0.3)
- Настройки инициализируются автоматически при первом запуске
- Все настройки можно редактировать через веб-интерфейс (вкладка "Системные настройки")

### Работа с базой данных

- База создается автоматически в директории `data` при первом запуске
- Все операции выполняются через SQLAlchemy ORM
- Поддержка транзакций и откатов при ошибках
- Каскадное удаление: при удалении записи истории удаляются все связанные статьи
- Возможность очистки базы через веб-интерфейс (на вкладке "История запросов")
- Возможность удаления отдельного запроса и его статей (кнопка "Удалить данные запроса")

## Настройки и параметры

### LLM настройки

- **OPENAI_API_KEY** - обязательный параметр, API ключ для доступа к LLM
  - **Для OpenAI**: ваш реальный ключ (начинается с `sk-...`)
  - **Для Ollama**: можно указать любое значение (например `ollama`) - Ollama игнорирует этот параметр
  - **Для других провайдеров**: ключ, предоставленный провайдером
- **OPENAI_API_BASE** - опционально, базовый URL для альтернативных провайдеров
  - Пример: `https://api.proxyapi.ru/openai/v1`
  - Система автоматически обрабатывает URL и добавляет необходимые пути
  - **Настраивается только через `.env`, не изменяется динамически из веб-интерфейса**
- **LLM_MODEL** - модель для использования (по умолчанию: `gpt-4o-mini`)
  - Можно изменить в веб-интерфейсе для текущего запуска
- **LLM_TEMPERATURE** - температура для генерации (0.0-2.0, по умолчанию: `0.7`)
  - Можно изменить в веб-интерфейсе для текущего запуска

**Примечание:** 
- Модель LLM и Temperature можно изменить в веб-интерфейсе в форме "Настройки обработки"
- API Endpoint настраивается только через `.env` и не изменяется динамически
- Изменения применяются только для текущего запуска обработки и не сохраняются в `.env`
- Каждый запуск создает новую запись в истории запросов с указанными параметрами

### Настройки дедупликации

- **SIMILARITY_THRESHOLD** - порог схожести (0.0-1.0, по умолчанию: `0.85`)
  - Чем выше значение, тем строже проверка на дубликаты
  - Можно изменить в веб-интерфейсе для текущего запуска
  - Рекомендуемые значения:
    - `0.9-1.0` - очень строгая проверка (почти идентичные статьи)
    - `0.8-0.9` - стандартная проверка (рекомендуется)
    - `0.7-0.8` - мягкая проверка (может находить похожие статьи)
    - `<0.7` - очень мягкая проверка (может находить не связанные статьи)

### Настройки embeddings

- **EMBEDDING_MODEL** - модель для генерации векторных представлений (по умолчанию: `text-embedding-3-small`)
  - Для OpenAI: `text-embedding-3-small` или `text-embedding-3-large`
  - Для Ollama: `snowflake-arctic-embed2` (рекомендуется) или `granite-embedding`
  - Настраивается только через `.env`

### Настройки базы данных

- **DATABASE_URL** - URL подключения к БД
  - SQLite: `sqlite:///data/news_agent.db` (по умолчанию)
    - База данных автоматически создается в поддиректории `data`
  - PostgreSQL: `postgresql://user:password@localhost/dbname`
  - MySQL: `mysql://user:password@localhost/dbname`

### Настройки веб-сервера

- **FLASK_HOST** - хост для веб-сервера (по умолчанию: `127.0.0.1`)
- **FLASK_PORT** - порт для веб-сервера (по умолчанию: `5000`)
- **FLASK_DEBUG** - режим отладки (по умолчанию: `False`)
- **FLASK_SECRET_KEY** - секретный ключ для сессий

### Использование Ollama (локальные модели)

Система поддерживает работу с Ollama для локального запуска моделей без использования облачных API.

**Установка Ollama:**
1. Скачайте и установите Ollama с официального сайта: https://ollama.com/download
2. Установите необходимые модели (см. `ollama_configuration.md` для рекомендаций)

**Настройка `.env` для Ollama:**
```env
# Для Ollama можно указать любое значение (например "ollama")
# Ollama игнорирует этот параметр, но библиотека требует его наличие
OPENAI_API_KEY=ollama
OPENAI_API_BASE=http://localhost:11434/v1
LLM_MODEL=qwen2.5:7b
EMBEDDING_MODEL=snowflake-arctic-embed2
```

**Важно:** 
- Для Ollama значение `OPENAI_API_KEY` может быть любым (например `ollama`, `local`, `dummy` и т.д.) - Ollama его игнорирует
- Для реального OpenAI API нужно указать ваш настоящий ключ (начинается с `sk-...`)

**Установка моделей:**
```bash
# LLM модель для классификации и саммари
ollama pull qwen2.5:7b

# Embedding модель для семантического поиска
ollama pull snowflake-arctic-embed2
```

**Особенности работы с Ollama:**
- ✅ Работает локально, без интернета
- ✅ Бесплатно, без лимитов на запросы
- ✅ Полный контроль над данными
- ⚠️ Требует достаточно RAM/VRAM (рекомендуется 16GB+)
- ⚠️ Может быть медленнее облачных API
- ⚠️ Качество может быть ниже, чем у GPT-4

**Проверка работы:**
```bash
# Проверьте, что Ollama запущен
ollama list

# Проверьте доступность API
curl http://localhost:11434/api/tags
```

**Рекомендации по моделям:** См. файл `ollama_configuration.md` для детальных рекомендаций по выбору моделей для ваших задач.

## Примеры использования

### Пример 1: Сбор новостей о технологиях

В веб-интерфейсе:
1. Проверьте текущие настройки системы в блоке "Текущие настройки системы"
2. Введите RSS каналы:
   ```
   https://techcrunch.com/feed/
   https://www.theverge.com/rss/index.xml
   https://arstechnica.com/feed/
   ```
3. Укажите критерий: `новости о технологиях, искусственном интеллекте и стартапах`
4. (Опционально) Измените настройки LLM или порог схожести для тестирования
5. Нажмите "Начать обработку"
6. Отслеживайте прогресс в реальном времени
7. Просмотрите релевантные новости в таблице

### Пример 2: Использование альтернативного API провайдера

**Вариант 1: Через .env (постоянная настройка)**
В `.env`:
```env
OPENAI_API_KEY=your_api_key
OPENAI_API_BASE=https://api.proxyapi.ru/openai/v1
LLM_MODEL=gpt-4o-mini
```

**Важно:** API Endpoint настраивается только через `.env` и не изменяется динамически из веб-интерфейса. Для изменения API endpoint необходимо отредактировать `.env` и перезапустить приложение.

### Пример 3: Использование семантического поиска

После обработки новостей:
1. В блоке "Семантический поиск" введите запрос, например: `искусственный интеллект и машинное обучение`
2. Настройте порог схожести (рекомендуется 0.7)
3. Нажмите "Найти"
4. Просмотрите результаты, отсортированные по схожести
5. Оценка схожести отображается рядом с оценкой релевантности

**Преимущества семантического поиска:**
- Поиск по смыслу, а не только по ключевым словам
- Находит статьи, даже если они используют другие формулировки
- Работает с уже обработанными статьями из базы данных

### Пример 4: CLI режим с настройками из .env

В `.env`:
```env
OPENAI_API_KEY=your_api_key
RSS_FEEDS=https://example.com/feed1.xml,https://example.com/feed2.xml
SELECTION_CRITERIA=новости о науке и технологиях
SIMILARITY_THRESHOLD=0.85
```

Запуск:
```bash
python main.py
```

## Технические детали

### Архитектура агентов

Система использует CrewAI для оркестрации агентов, хотя основная логика реализована через прямые вызовы функций. Агенты создаются для совместимости с CrewAI фреймворком.

### Обработка ошибок

- При ошибке парсинга RSS канала - пропуск канала, продолжение обработки
- При ошибке LLM API - fallback на простую классификацию по ключевым словам
- При ошибке генерации саммари - fallback на простое саммари из первых предложений, продолжение работы
- При ошибке генерации embeddings - пропуск embeddings, продолжение работы (семантический поиск будет недоступен)
- При ошибке сохранения в БД - откат транзакции, логирование ошибки
- Все ошибки логируются в консоль сервера
- Система продолжает работу даже при частичных сбоях

### Безопасность

- Санитизация HTML контента через DOMPurify
- Защита от XSS атак в веб-интерфейсе
- Валидация входных данных
- Безопасное хранение API ключей в переменных окружения

### Производительность

- Асинхронная обработка в фоновых потоках
- Индексация по content_hash для быстрого поиска дубликатов
- Пакетная обработка статей
- Оптимизированные запросы к БД

## История запросов

Система автоматически сохраняет историю всех поисковых запросов:

- **Автоматическое сохранение**: Каждый запуск поиска создает новую запись в истории
- **Связь со статьями**: Все статьи связаны с запросом через `search_history_id`
- **Сохранение параметров**: В истории сохраняются все параметры запроса:
  - RSS каналы
  - Критерий отбора
  - Настройки LLM (модель, temperature)
  - Порог схожести
  - API endpoint
- **Сохранение результатов**: Статистика сохраняется в JSON поле `results_data`
- **Управление**: 
  - Просмотр истории на отдельной вкладке
  - Удаление отдельного запроса и всех связанных статей
  - Полная очистка базы данных

## План развития

### Высокий приоритет (ближайшие улучшения)

#### 1. Кластеризация новостей по темам
- **Цель**: Группировка похожих статей в кластеры для лучшей навигации
- **Реализация**: 
  - Использование embeddings для кластеризации (K-Means, DBSCAN)
  - Автоматическое определение тем кластеров
  - Отображение кластеров в UI с возможностью развернуть/свернуть
- **Преимущества**: Видно развитие одной темы из разных источников, удобная навигация

#### 2. Экспорт данных
- **Цель**: Возможность экспорта результатов в различные форматы
- **Реализация**:
  - Экспорт в CSV (таблица статей)
  - Экспорт в JSON (полные данные)
  - Экспорт в PDF (форматированный отчет)
  - Генерация RSS-фида отфильтрованных новостей
- **Преимущества**: Интеграция с другими системами, архивирование результатов

#### 3. Улучшенная аналитика и визуализация
- **Цель**: Расширенная аналитика с графиками и трендами
- **Реализация**:
  - Графики трендов по времени (количество статей, релевантность)
  - Топ источников по релевантности
  - Анализ популярности тем
  - Временные паттерны публикаций
  - Интеграция Chart.js или Plotly
- **Преимущества**: Лучшее понимание данных, выявление трендов

### Средний приоритет

#### 4. Персонализация и рекомендации
- **Цель**: Система рекомендаций на основе истории и предпочтений
- **Реализация**:
  - Сохранение предпочтений пользователя
  - Обучение на основе действий (просмотры, релевантность)
  - Приоритизация интересных тем
  - Персонализированные дайджесты
- **Преимущества**: Более релевантные результаты, экономия времени

#### 5. Кэширование и оптимизация производительности
- **Цель**: Улучшение скорости работы и снижение затрат на API
- **Реализация**:
  - Кэширование результатов LLM запросов
  - Batch-обработка для экономии API-вызовов
  - Фоновая обработка RSS-каналов
  - Оптимизация запросов к БД
  - Индексация embeddings для быстрого поиска
- **Преимущества**: Быстрее работа, меньше затрат на API

#### 6. Улучшенный UI/UX
- **Цель**: Современный и удобный интерфейс
- **Реализация**:
  - Темная тема
  - Расширенные фильтры и сортировка
  - Закладки/избранное для статей
  - Быстрый поиск по базе (полнотекстовый)
  - Адаптивный дизайн для мобильных устройств
  - Клавиатурные сокращения
- **Преимущества**: Удобство использования, современный вид

### Низкий приоритет (долгосрочные планы)

#### 7. Мультиязычность
- **Цель**: Поддержка нескольких языков
- **Реализация**:
  - Автоопределение языка статьи
  - Перевод заголовков и саммари
  - Интерфейс на нескольких языках
  - Поиск на разных языках
- **Преимущества**: Работа с международными источниками

#### 8. Мониторинг источников
- **Цель**: Отслеживание состояния RSS-каналов
- **Реализация**:
  - Уведомления о недоступных каналах
  - Статистика по источникам (частота обновлений, количество статей)
  - Автоматическое обнаружение новых каналов
  - Проверка валидности RSS-фидов
- **Преимущества**: Надежность системы, автоматизация

#### 9. Интеграции и API
- **Цель**: Интеграция с внешними системами
- **Реализация**:
  - RESTful API для внешних приложений
  - Webhooks для уведомлений
  - Интеграция с Telegram/Discord ботами
  - Email-дайджесты
  - Экспорт в другие форматы (Markdown, HTML)
- **Преимущества**: Расширение возможностей использования

#### 10. Планировщик задач
- **Цель**: Автоматический сбор новостей по расписанию
- **Реализация**:
  - Настройка расписания (cron-like)
  - Автоматический запуск обработки
  - Уведомления о новых релевантных новостях
  - История автоматических запусков
- **Преимущества**: Полная автоматизация процесса

### Технические улучшения

- [ ] Миграции базы данных (Alembic)
- [ ] Unit и интеграционные тесты
- [ ] Логирование в файлы
- [ ] Мониторинг производительности
- [ ] Docker контейнеризация
- [ ] CI/CD pipeline
- [ ] Документация API (Swagger/OpenAPI)
- [ ] Поддержка PostgreSQL/MySQL для продакшена
- [ ] Rate limiting для API
- [ ] Аутентификация пользователей (если нужна)

### Улучшения алгоритмов

- [ ] Использование более продвинутых моделей embeddings (multilingual)
- [ ] Fine-tuning моделей на доменных данных
- [ ] Улучшение алгоритма дедупликации с использованием embeddings
- [ ] Автоматическое определение категорий новостей
- [ ] Определение тональности (sentiment analysis)
- [ ] Извлечение именованных сущностей (NER)
- [ ] **Словарь синонимов для keyword matching** (низкий приоритет)
  - Добавление расширяемого словаря синонимов для улучшения keyword matching
  - Поддержка склонений и вариаций слов (например: "виза" → "визы", "визу", "визой")
  - Интеграция с морфологическим анализатором для автоматического определения синонимов
  - **Примечание**: В текущей реализации семантический поиск через embeddings является основным механизмом и автоматически обрабатывает синонимы, поэтому словарь синонимов имеет низкий приоритет

