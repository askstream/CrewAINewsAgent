# OpenAI API ключ
# Для OpenAI: укажите ваш реальный ключ (начинается с sk-...)
# Для Ollama: можно указать любое значение (например "ollama") - Ollama игнорирует этот параметр
# Для других провайдеров: укажите ключ, предоставленный провайдером
OPENAI_API_KEY=your_openai_api_key_here

# Базовый URL API (опционально, для использования альтернативных провайдеров)
OPENAI_API_BASE=https://openai.api.proxyapi.ru/v1

# Модель LLM
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.7

# Модель для embeddings (векторные представления)
# Для OpenAI: text-embedding-3-small
# Для Ollama: snowflake-arctic-embed2 или granite-embedding
EMBEDDING_MODEL=text-embedding-3-small

# База данных
DATABASE_URL=sqlite:///data/news_agent.db

# RSS каналы (разделенные запятыми)
RSS_FEEDS=https://example.com/feed1.xml,https://example.com/feed2.xml

# Критерий отбора новостей
SELECTION_CRITERIA=новости о технологиях и искусственном интеллекте

# Порог схожести для дедупликации (0.0 - 1.0)
SIMILARITY_THRESHOLD=0.85

# Порог релевантности для классификации (0.0 - 1.0)
# Статья считается релевантной, если relevance_score >= RELEVANCE_THRESHOLD
RELEVANCE_THRESHOLD=0.6

# Flask настройки (опционально)
FLASK_SECRET_KEY=dev-secret-key-change-in-production
FLASK_DEBUG=False
FLASK_HOST=127.0.0.1
FLASK_PORT=5000

